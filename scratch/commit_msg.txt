feat(core): complete vibrant command center UI/UX overhaul, 4-tier timetable fallback engine & production sync

Summary of engineering changes and milestones completed in the last 24 hours:

1. Vibrant Railway Network Command Center UI/UX Overhaul:
   - Full visual migration from generic corporate-white layout to a high-density, mission-critical operations console.
   - Design System Tokens: Midnight Navy base (#0B1220, #0F172A), Indian Railways Crimson Red (#EF3340, #DC2626), Electric Cyan (#22D3EE), Amber warning (#F59E0B), and Emerald signal (#10B981).
   - Zero-UI-leakage strict boundary enforcement: Applied border-box, overflow hidden, and high-contrast styling across desktop (1920px), tablet (768px), and mobile (360px) viewports.
   - Interactive SVG Topology Graph: Animated glowing node boundaries, route pulses, and high-legibility station markers in js/app.js.
   - Component Modernization: Re-architected Top Navbar with pulsating Live Pipeline telemetry status, slide-out Station Intelligence Drawer, Journey Planner, Timetable Modal, AI Assistant Drawer, and PNR Tracker.

2. Four-Tier Timetable Fallback Engine & Data Resilience:
   - Eliminated serverless SQLite runtime isolation bottlenecks ('Train query failed') on static/CDN hosts.
   - Decomposed master SQLite database into 5,208 discrete train stoppage sequences (DATA/trains/<trainNumber>.json) and a 7,358-train catalog (DATA/train_catalog.json).
   - Implemented 4-tier timetable retrieval logic:
     * Tier 1: Local SQLite API (/api/timetable/:id)
     * Tier 2: Static CDN JSON stoppage sequence (DATA/trains/${id}.json)
     * Tier 3: In-memory global train catalog & station registry
     * Tier 4: Dynamic synthetic route interpolation failsafe
   - Configured vercel.json rewrite rules and aggressive browser caching (max-age=86400).

3. Multi-Target Directory Synchronization:
   - Synchronized all production assets, CSS tokens, JS engines, and JSON datasets across root, deploy/, git/, frontend/, and src/main/resources/static/.
   - Automated sync pipeline via scratch/sync_folders.ps1.

4. Master Documentation & PBL Academic Evidence:
   - Documented Days 13 & 14 in project_build_history.md with root cause analysis and mitigation entries #9, #10, and #11.
   - Updated spec.md Section 7.7 and README.md Presentation Tier to reflect the Command Center design and resilient data architecture.
   - Verified 100% JUnit 5 test suite pass rate (20/20 tests).
